#include <stdio>

/**
 * main Prints numbers from 0 to 9
 *
 * Return: Always 0 (Successs)
 */
int main(void)
{
	int i;
	
	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
