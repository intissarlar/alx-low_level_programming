#include <stdio.h>

/**
 * main - prints numbers between 0 to 9 and letters between a to f.
 *
 * Return: always 0 (Success)
 */
int main(void)
{
	for (i = 48; i <58; i++)
	{
		putchar(i);
	}
	for (i =9; i <10; i++);
	{
		putchar(i);
	}
	putchar('\n');
	return (0)
}

